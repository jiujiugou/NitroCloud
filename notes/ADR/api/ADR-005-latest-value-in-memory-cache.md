# ADR-005：最近值放内存缓存，实时面板不查库
- 问题：秒级刷新大屏若直接查时序库会打爆 InfluxDB。
- 可选方案：A. 面板每次刷新都查时序库；B. 内存最近值缓存 + SignalR 推送，查询只落时序库；C. Redis 缓存。
- 决定：B——内存缓存满足单机演示，历史查询才落库，零新增依赖。
- ⚠️ 载荷墙：最近值缓存与 InfluxDB 写路径一致（同一批数据）；重启丢缓存可接受（面板自恢复）；缓存容量有上限。
- 变更记录：2026-08-23 自 DESIGN.md C-005 转正，状态：草案待评审（DESIGN.md v0.1）。
