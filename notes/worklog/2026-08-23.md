# 2026-08-23

## 当前目标
- MQTT 接入连接增强完成（连接状态判定 + Polly 重试 + OperationalError + 日志 + 指标）；ingest 模块 10 个源文件 XML 注释补全完成（仅注释，0 行为改动）；下一步为集成测试与前端联调。

## ingest 模块注释补全（本轮）
- 需求：为 ingest 模块补充详细 XML 注释（AGENTS.md 规则 7：含义/默认值/边界/设计意图写进注释，随代码维护）。**只改注释，未改任何行为/接口/依赖**。
- 覆盖 `src/NitroCloud.Ingest` 全部 10 个 `.cs`：`Parsing/MeasurementBatchParser.cs`、`Parsing/AlarmPayload.cs`、`Parsing/UtcDateTimeConverter.cs`、`IngestOptions.cs`、`IngestServiceCollectionExtensions.cs`、`BatchDeduplicator.cs`、`IngestRetryQueue.cs`、`MeasurementPipeline.cs`、`MqttConnectRetryPolicy.cs`、`MqttIngestHostedService.cs`。
- 注释风格：类注释给职责边界 + 设计要点（含线程安全/并发说明/ADR 引用）；属性注释给含义/默认值/边界/钳制；方法注释给处理步骤 + `<param>` + `<returns>` 语义。
- 关键补充点（示例）：`IngestOptions` 各属性补钳制说明（消费方下限钳制为 1 / 0）；`IngestRetryQueue` 补 Channel 线程安全与退避边界（基数钳 1 天、指数钳 2^20）；`MqttIngestHostedService` 补 `_pending` 锁用途、`_disconnected` TCS 语义（每次连接循环前重建）、告警映射回退（severity→Warning、state 非 Resolved→Active）、scope 解析原因、三条并行循环协调。
- 验证：`dotnet build NitroCloud.slnx` 0 警告 0 错误；`dotnet test` 69 通过 / 6 失败——6 项仍为既有失败（`MeasurementPipelineTests` 5 + `__DiagTests` 1，根因 `MeasurementRecord.SiteId` required 与上行载荷不含 siteId，与本次注释改动无关，见下方「既有问题」）。

## MQTT 接入连接增强（Polly 重试，本轮）
- 需求：对 MQTT 增加连接状态判定；连接不上用 Polly 重试（次数仍为 3）；仍连不上把错误保存进 `OperationalError` 并输出错误日志。聚焦 Ingest `MqttIngestHostedService`（Command MQTT 已具备状态判定/OperationalError/日志，未改动）。
- 新增依赖：`Polly` 8.7.0（用户明确要求，非升级/降级既有包）。
- `IngestOptions.MqttConnectMaxRetries` 默认 3；`MqttConnectRetryPolicy`（新文件，Polly 8 封装，独立可单测）：连接/订阅失败按固定退避重试（间隔复用 `ReconnectDelayMs`），重试耗尽仍失败把 Communication 类 `OperationalError` 装进 `OperationResult` 返回；重试 0 次退化为直通（Polly 校验 `MaxRetryAttempts>=1`，已特判）。
- `MqttIngestHostedService`：拆出 `ConnectAndSubscribeAsync`（新增状态判定：`ConnectAsync` 后检查 `ResultCode != Success` 即失败；异常也转 `OperationalError.Communication`）；`RunMqttLoopAsync` 经 `MqttConnectRetryPolicy.ExecuteAsync` 驱动，失败路径 `_logger.LogError` 打印错误 + `CloudMetrics.MqttConnectFailureTotal`（新增 `cloud_mqtt_connect_failure_total`，对齐 ADR-008 D8「MQTT 重连次数」）。
- 单元测试：`tests/NitroCloud.UnitTests/Ingest/MqttConnectRetryPolicyTests.cs` 新增 5 项（重试 3 次共 4 次尝试、0 重试直通、成功短路、取消不重试、MaxRetries 暴露），全绿。
- 验证：`dotnet build NitroCloud.slnx` 0 警告 0 错误；`dotnet test` 69 通过 / 6 失败——6 项为**既有失败，与本轮无关**（详见下方「既有问题」）。

## 既有问题（本轮发现，未修，超出本任务范围）
- `MeasurementPipelineTests`（5 项）+ `__DiagTests`（1 项）失败：`MeasurementRecord.SiteId` 为 `required`，但上行载荷 `records[]` 内单条记录不带 `siteId`（siteId 在批次顶层，解析器计划反序列化后再注入 topic 第三段 siteId，见 `MeasurementBatchParser.Parse` 注释）。`required` 使 System.Text.Json 在注入前就抛 `JsonException`。影响 M1 DoD「模拟网关数据能进 InfluxDB」，建议单独排期（涉及上行契约/解析器，需 G1 确认行为变更）。

## 后端 M1 骨架（本轮）
- 按 ADR-008 D1/D2 落地 `src/` 骨架：Domain（纯 C# 领域模型）/ Shared（OperationResult、TimeUtil、TopicUtil、ValueCoercion）/ Storage（ITimeseriesStore、ILatestValueCache、IAlarmStore、ICommandStore、IRealtimeNotifier 纯接口）/ Persistence（SQLite + FluentMigrator 三表迁移）/ Influx（InfluxTimeseriesStore + BatchWriter + FluxQueryBuilder）/ Ingest（MqttIngestHostedService + 解析/去重/重试队列）/ Command（ICommandDispatcher + MqttCommandClient + CommandManager 超时重试）/ Api（REST 六控制器 + SignalR Hub + 组合根 + 健康检查）/ Telemetry（Serilog + Prometheus + Activity）。
- 单宿主：唯一宿主 = `NitroCloud.Api`；Ingest/Command 以 `BackgroundService` 注册进同一宿主；Api 组合根注入 Persistence/Influx/Ingest/Command。推送解耦：Ingest/Command 只依赖 `Storage.IRealtimeNotifier`，SignalR 实现由 Api 提供（ADR-008 D1）。
- 关键契约落代码：上行 `nitrogateway/{siteId}/{deviceId}/measurements|alarms`、下行 `commands|commands/ack`；无 v 字段按 v1 兼容；载荷 siteId 与 topic 第三段冗余校验记警告不失败（ADR-004）；`ApiResponse<T>` 外壳；SignalR Hub `/hubs/cloud` 分组 `site:{siteId}`；命令回执 `commands/ack` → Acked/Failed，超时重试上限 3 次不换 commandId（ADR-008 D6）。
- 兼容修复（勿回退）：MQTTnet 5.1.0.1559（无 `MqttFactory`，用 `MqttClientFactory`，载荷 `ReadOnlySequence<byte>` 逐段拷贝）；InfluxDB.Client 5.1.0（`Query.Create` 已移除，用 string 重载）。
- 单元测试：`tests/NitroCloud.UnitTests` 新增 63 项（TopicUtil / ValueCoercion / TimeUtil / MeasurementBatchParser / BatchDeduplicator / IngestRetryQueue / FluxQueryBuilder / InMemoryLatestValueCache / OnlineStatusService），`dotnet test` 全绿；IntegrationTests 项目留待联调阶段填充。
- 验证：`dotnet build NitroCloud.slnx` 0 警告 0 错误；`dotnet test NitroCloud.slnx` UnitTests 63/63 通过。
- 未提交 git；是否提交由用户决定（AGENTS.md 规则 6）。

## 前端落地（早前）
- 按 ADR-009 D2 目录落地 `web/src`：api/（client、signalr、types、sites/devices/points/history/alarms/commands）、composables/（useRealtimeSite / useLatestValues / useAlarmFeed / useCommand）、components/（KpiCard/SiteCard/PointRow/RealtimeChart/AlarmTicker/CommandDialog/StatusLight）、views/dashboard + views/admin（Sites/Devices/Points/Alarms/History）、styles/（base + theme-dark/theme-light）、router/index.ts、App.vue、main.ts、vite-env.d.ts。
- 大屏 5 块核心（KPI / 站点总览 / 实时曲线 / 告警滚动 / 写值闭环）：REST 快照 + SignalR `OnMeasurements/OnAlarm/OnDeviceStatus/OnCommandAck` 增量，重连补拉 `/latest`（防白屏），ECharts 500ms 节流 + 7200 点环形缓冲 + `step:'end'`；管理面板 CRUD、告警确认、历史查询 + CSV 导出。
- 关键契约（落代码，后端需同步）：
  - SignalR Hub 定为 `/hubs/cloud`，入站 `SubscribeSite/UnsubscribeSite`，出站 `OnMeasurements/OnAlarm/OnDeviceStatus/OnCommandAck`（ADR-008 D7 NitroCloudHub）。
  - 写值 POST `/api/commands/write` → 回执 OnCommandAck + 10s 超时轮询兜底；`useCommand.send` 返回 `CommandStatus | 'Error'`（'Error' 仅前端提交阶段网络失败，非后端状态）。
  - 告警汇总 `GET /api/alarms/summary` 失败时按 occurAt 本地统计兜底。
- 决策验证：不引 Pinia（ADR-009 D5）——大屏 composables + admin 局部状态，首轮构建验证够用。
- 修复 3 处既有类型问题：AlarmTicker 未捕获 defineProps（siteName 引用未定义）、useCommand 返回 'Error' 超出 CommandStatus、DashboardView 未解包 hubConnected ref（StatusLight 入参）。
- 未提交 git；是否提交由用户决定（AGENTS.md 规则 6）。

## 设计阶段（早前结论）
- 参照 NitroGateway 既有惯例（AGENTS.md 五段式 + notes/ADR 按模块归档 + worklog），为 NitroCloud 落地同样脚手架。
- DESIGN.md 的 7 条「关键设计决策（ADR 预期）」C-001..C-007 转正为 ADR-001..ADR-007，按模块归入 storage/ ingest/ command/ architecture/ api/，状态「草案待评审」。
- 新增 ADR-008（architecture/，后端初版设计：slnx 骨架 ・ 模块职责 ・ Storage 五接口 ・ 上下行数据流 ・ API 清单 ・ 可观测性）、ADR-009（web/，前端初版设计：大屏+管理面板 ・ 实时订阅 ・ 曲线复用 ・ 写值闭环），并同步 ADR 地图与本章。
- `docs/` 建 README 索引，随 M1 骨架后回填 01~04 认识文档、数据契约、部署演示、面试条目。
