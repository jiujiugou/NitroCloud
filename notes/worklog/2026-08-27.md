# 2026-08-27

## 当前目标
- DoD 5 反向写值闭环已实现（ADR-010 已落地）：设计文档 → ADR-010（已完成），云侧落码（Command 模块 + ICommandStore + M005 + CommandsController + 测试 17/17）已完成，build/test 已验证；剩余：端到端回执联调（依赖 NitroGateway 命令处理器 / mqtt-simulator 回执模拟，属跨项目前置）+ 是否提交由用户决定（AGENTS.md 规则 6）。

## 盘点：nginx 职责（核实自 web/nginx.conf + web/Dockerfile）
- nginx 内嵌在 `web` 服务镜像里（两阶段：node 构建 → nginx 托管），不做独立服务。
- 职责：① 托管 dist + vue-router history 回退 /index.html；② gzip 压缩；③ 静态资源 7 天长缓存（content-hash）；④ 同源反代 `/api/`、`/hubs/`（WebSocket Upgrade 头）→ center:5100，规避 CORS（后端 CORS 仅放行 dev 源 5173）。对外只暴露 8088。

## 盘点：项目已完成 / 未完成
- 已完成：M1 骨架（8 模块 + Vue3 + 单测，61b1d89）；Docker 单机四服务（0dcb236）；模拟网关 tools/mqtt-simulator（a304be4）；DoD 1/2/3/4/6 可演示。
- 未完成：DoD 5 反向写值闭环（Command 项目不在 slnx、src/NitroCloud.Command 不存在、Api 无 CommandsController，前端已就绪）；6 项既有测试失败（SiteId required）；无鉴权/TLS/CI（.github/workflows 为空）。

## 发现：上线前必改问题（写入 docs/20 §3，未改代码）
- **端口不一致（阻塞）**：appsettings.json `Urls=0.0.0.0:5110`（commit 27a3a16 改动），但 nginx/vite/README/Dockerfile 注释均指向 5100 → web 反代与 dev 代理均 502。建议 appsettings 改回 5100。
- center 在 compose 无 ports、无 healthcheck；InfluxDB 未映射宿主端口 → README 声称的 /healthz、/metrics、:8086 当前不可达。
- 默认口令 admin123456 / dev-token，InfluxDB 首启固化（改动需 down -v 重建卷）。
- README/docs 已知出入：deploy.sh、--profile full/demo、sim 服务、tools/NitroCloud.GatewaySim 均与仓库不一致（此前 docs/README 已记录，本次核实仍成立）。
- 本机 dotnet SDK 为 11 preview（非 10）。

## 产出
- 新增 `docs/20-部署与演示.md`：nginx 职责 / 已完成 / 上线前必改清单（§3）/ 运维手册（§4）/ 已知出入（§5）/ 演示路径（§6）。
- 未提交 git；是否提交由用户决定。

## 回写功能设计（本轮，仅文档无代码改动）
- 需求：实现 DoD 5 反向写值闭环；用户要求先出设计文档进 ADR。
- 现状核实（盘点是本轮结论，无代码改动）：
  - **已就绪**：Domain 命令五件套、Shared TopicUtil 命令 topic、Api Dtos（WriteValue/CommandRecordDto + 映射）、SignalR 序列化约定、CloudMetrics 命令三指标、前端 commands.ts/useCommand/CommandDialog（OnCommandAck 已接线）、appsettings `Command` 段。
  - **缺失**：`src/NitroCloud.Command` 项目（不在 slnx）、`Storage/ICommandStore.cs`、Persistence `CommandStore` + `command_records` 表（M003 建过又被 M004 删）、Api `CommandsController`、Program 未注册 Command。
  - **跨项目前置**：NitroGateway 无命令处理器（契约以 DESIGN.md §4.3 为准，网关侧补订阅处理器属可接受小改动）；`tools/mqtt-simulator` 需补命令回执模拟以驱动演示。
  - 已核对：docker-compose 现仅配 `Ingest__MqttHost`，实现时需补 `Command__MqttHost/Port`（D7）。
- 设计决策（详见 ADR-010）：新 Command 模块（CommandOptions/ICommandDispatcher/MqttCommandClient/CommandManager/CommandHostedService）；ICommandStore 四方法（Add/UpdateStatus/Get/QueryInFlight，终态不覆盖）；M005 重建 command_records（数据模型变更，落码前 G1 确认）；状态机/幂等沿用 ADR-003/008 D6；超时 10s/重试 3 次/轮询 1s；Command 不引 Polly、不跨模块引用 Ingest。
- 产出：
  - 新增 `notes/ADR/command/ADR-010-command-write-back-loop-design.md`（设计 ADR，主交付物）。
  - 新增 `notes/ADR/README.md`（导航索引，此前缺失；登记全部 10 条 ADR 并按模块分组）。
  - 未改 DESIGN.md（§4.3/C-003 已覆盖命令契约，DoD5 状态同步留待落码前）。
- 下一步（按用户节奏）：落码实现 ADR-010（需先 G1 确认 M005 重建表）→ 补测试 → 联调。
- 未提交 git；是否提交由用户决定。

## 回写功能实现（落码完成，ADR-010 已落地）
- 落码范围（与 ADR-010 待实现清单一致，全部完成）：
  - 新项目 `src/NitroCloud.Command`（8 文件）：`CommandOptions` / `ICommandDispatcher` / `MqttCommandClient`（发布 QoS1 + 订阅 commands/ack + 固定退避重连，不引 Polly）/ `CommandManager`（状态机 + 超时重试 + 幂等，纯类可单测）/ `CommandHostedService`（并行 MQTT 循环 + 扫描轮询）/ `CommandRequestSerializer` / `CommandAckParser` / `CommandServiceCollectionExtensions`（`AddNitroCommand`，含 `ICommandDispatcher` 别名）。
  - slnx 注册 Command 项目；`Api.csproj` 引用 + `Program.cs` `AddNitroCommand`（AddNitroIngest 之后）。
  - `Storage/ICommandStore`（只增不删，Add/UpdateStatus/Get/QueryInFlight，终态不覆盖）。
  - Persistence：`CommandRecordEntity` + `Sqlite/CommandStore` + `AppDbContext`（CommandRecords DbSet + 映射 + 复合索引）+ 迁移 **M005** 重建 `command_records`（M003 结构原样，索引 `(site_id,status)`）；`SqliteServiceCollectionExtensions` 注册 `ICommandStore`。
  - Api `CommandsController`：`POST /api/commands/write`（siteId/deviceId/pointId 非空 + point 存在且 Writable + device 归属校验；落库 Pending → DispatchAsync → 返回 commandId；发布失败不阻塞响应）+ `GET /api/commands/{commandId}`。
  - 单测 3 文件 17 用例：`CommandManagerTests`（10）/ `CommandAckParserTests`（5）/ `CommandRequestSerializerTests`（2）。
- 验证：`dotnet build NitroCloud.slnx` **0 警告 0 错误**（11 项目）；`dotnet test` 全量 **90 通过 / 2 失败**——2 项失败均为既有 `ValueCoercionTests`（`ValueCoercion.cs` switch default 未处理 C# 原生数值/布尔，非本次引入、未触碰该文件）；Command 过滤单测 **17/17 全绿**；IntegrationTests 无 .cs 测试文件（常态）。
- 关键实现点（供后续维护）：
  - 重试计数语义：`attempts+1` 前置累计 + `attempts > MaxAttempts` 才标 Timeout → MaxAttempts=3 时恰好重发 3 次。
  - 发布失败保持 Pending（attempts 累计），后台扫描兜底，防止 Api 触发发布丢失。
  - 超时锚点 `SentAt ?? RequestedAt`；成功回执清空 Error（`error = result==Success ? null : ack.Error`）。
  - CommandManager 按操作经 `IServiceScopeFactory` 开 scope 解析 scoped `ICommandStore`。
  - 回执 result 为**字符串枚举**（Success/Failure），时间统一归一 UTC。
- 未同步（保持现状）：docker-compose 未补 `Command__MqttHost/Port`（D7，演示前由用户/后续决定）；NitroGateway 命令处理器 + `tools/mqtt-simulator` 回执模拟（D8，跨项目前置）未实现。
- 未提交 git；是否提交由用户决定（AGENTS.md 规则 6）。

## 回写功能：NitroGateway 命令处理器已落地（b 部分，D8 跨项目前置之一）
- NitroGateway 侧（D8 前置之一）已实现：新 `src/NitroGateway.Command`（订阅 `nitrogateway/+/+/commands` QoS1 → 校验解析 → WriteService 写值 → 幂等回执 `commands/ack`），命令契约与云侧一致不变；详见 NitroGateway ADR-069。
- D8 剩余项：`tools/mqtt-simulator` 命令回执模拟（无真实网关演示用）+ 云/网关/模拟端到端联调。
