# Worklog 2026-08-30

## 当前目标
- 修复历史查询 Flux 时间参数 bug（HistoryController → InfluxTimeseriesStore → FluxQueryBuilder）：`range(start, stop)` 的时间被双引号包裹成 string，InfluxDB 报 "value is not a time, got string"；改为裸 RFC3339 time 字面量，并补 `From > To` 校验与单测。

## 落地内容
- `src/NitroCloud.Influx/FluxQueryBuilder.cs`：`Rfc3339`（给时间加双引号）改为 `FormatTimeLiteral`（返回不带引号的 RFC3339 UTC 字面量，`ToString("O")` 保留 7 位小数 + Z）；`Build` 构造前校验 `From > To` 抛 `ArgumentException`。
- `tests/NitroCloud.UnitTests/Influx/FluxQueryBuilderTests.cs`：既有断言改为无引号格式；新增 4 条单测（无引号回归、`From > To` 抛异常、`From == To` 边界、UTC/Z 结尾无偏移）。
- `HistoryController` / `TimeseriesQuery` 类型检查无问题（`DateTime` UTC），未改动；controller 原有 `from <= to` 校验保留，构建器校验属纵深防御。

## 验证
- 红绿对照：改测试后先跑 `dotnet test`（FluxQueryBuilderTests 5 条失败）→ 修复后再跑（13 条全绿）。
- `dotnet build NitroCloud.slnx -c Debug`：0 错误 0 警告。
- `dotnet test NitroCloud.slnx -c Debug --no-build`：UnitTests 114 通过、IntegrationTests 6 通过，全绿。

## 备注
- 未改依赖、未改网关契约；未 git 提交（规则：默认由用户执行）。

---

## 上一条（已完成）：元数据只读约束

## 当前目标
- 元数据管理只读约束落地：站点/设备/点位由上行数据自动注册（ADR-013）驱动，禁止手动新增/删除；保留编辑（改名/补全）与写值能力（命令下发）。

## 决策
- ADR-017 元数据只读约束（站点/设备/点位禁止手动新增/删除，由自动注册驱动）—— 已落地。
- 采用「配置开关 `Metadata:AllowManualCreate`（默认 false）+ 403」而非删端点：可回退、可测试。
- 大屏写值链路（Command/回执）与匿名只读端点不动；`Storage/` 接口不增删；不改网关契约。

## 落地内容
- 后端 `src/NitroCloud.Api/ApiOptions.cs` 新增 `MetadataOptions`；`Program.cs` 绑定 `Metadata` 配置节；`appsettings.json` 默认 `AllowManualCreate: false`。
- `SitesController` / `DevicesController` / `PointsController`：`POST`（新增）与 `DELETE`（删除）在关闭开关时返回 `403 MetadataReadOnly`；`PUT`（编辑）不受影响。
- 前端 `SitesView.vue` / `DevicesView.vue` / `PointsView.vue`：移除新建/删除按钮与对话框，清理相关 import，保留编辑；大屏与写值入口未动。
- 测试：`tests/NitroCloud.UnitTests/Api/MetadataReadOnlyConstraintTests.cs`（6 条 403 断言）；`tests/NitroCloud.IntegrationTests/MetadataReadOnlyTests.cs`（开关开启时可手动新增 + 改名 PUT 可用，落库校验）。

## 验证
- `dotnet build NitroCloud.slnx -c Debug`：0 错误 0 警告。
- `dotnet test NitroCloud.slnx -c Debug --no-build`：UnitTests 110 通过、IntegrationTests 6 通过，全绿。
- `cd web && npm run build`：vue-tsc + vite 构建通过（仅有 chunk >500kB 的既有体积提示，非错误）。

## 备注
- 未 git 提交（规则：默认由用户执行）。
- 部署如需回退手动新增能力，用环境变量 `Metadata__AllowManualCreate=true` 覆盖；默认不覆盖。
