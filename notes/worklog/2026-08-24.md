# 2026-08-24

## 当前目标
- M1 骨架 + Docker 单机部署已提交（61b1d89 / 0dcb236），center/web Dockerfile 与 nginx 反代已就绪；`docker compose up -d --build` 可拉起四服务。
- 下一步：补齐 docs/ 认识文档（01~04 / 10 / 30）；实现 DoD 5 Command 模块；排期修 `MeasurementRecord.SiteId` required 与上行契约的 6 项既有测试失败（需 G1 确认）。
## 初始提交并推送（本轮，AI 代执行）
- 处理了 src/ 下误建的嵌套 git 仓库（src\.git，无 commit 无 remote），删除后由外层仓库统一管理整个项目。
- 补充 .gitignore 增加 **/logs/ 规则（AGENTS.md 要求 logs/ 不提交），已 amend 进初始提交。
- 提交：61b1d89（初始提交 NitroCloud M1 骨架，后端 8 模块 + Vue 3 前端 + 单元测试，共 178 个文件）
- 远端：origin = https://github.com/jiujiugou/NitroCloud.git，分支 master，-u origin/master 跟踪，force-with-lease 推送（amend 后）
- 结果：* [new branch] master -> master 成功，远端 HEAD = 61b1d89，本地工作区干净。

## 完善 docker-compose.yml（本轮）
- 依据 DESIGN.md §2.3 部署形态 + DoD 6「一条命令拉起」，保持单机 4 服务（broker + influx + center + web），未扩范围（Prometheus 未加服务，/metrics 已暴露供外部抓取）。
- 敏感项集中到 .env（compose 用 ${VAR:-默认值}，未建 .env 也一条命令拉起）：新增 .env.example（提交），.env 不提交（AGENTS.md/.gitignore 已预留）。已用内联环境变量验证覆盖传播。
- 补 center 健康检查（/healthz，curl；注明 Dockerfile 需安装 curl）并让 web 等 service_healthy，消除 502 窗口；broker/influx 沿用原有 healthcheck。
- 显式 
itrocloud-net 网络 + TZ: Asia/Shanghai；刷新注释引用 ADR-001/D5/D6/D8。
- 新增根 .dockerignore 与 web/.dockerignore（否则 center 构建会把 .git/node_modules 送进上下文）。
- 保留 Command__MqttHost/Port（前瞻性配置，Command 模块未实现，注明 ADR-003/D6）。
- 验证：docker compose config 与 docker compose --profile full config 均通过（EXITCODE=0），默认值与 .env 覆盖均正确。
- 待补：src/NitroCloud.Api/Dockerfile、web/Dockerfile（含 nginx 反代 /api、/hubs → center:5100）尚未创建。

## 生成 docs/README.md（本轮）
- 需求：docs/README.md 停在 2026-08-23「未开工」状态，与实际脱节；重写为准确反映当前仓库的 docs/ 索引（纯文档，无行为变更）。
- 已更新：现状（M1 骨架 + Docker 部署提交哈希）、文档索引表（20 已建，01~04/10/30 待建）、三条约定（保留原文）。
- 发现（未改代码，记入 docs/README 现状「已知出入」）：根 README.md 与 docs/20-部署与演示.md 描述的 `deploy.sh`、`docker compose --profile full/demo`、`sim` 服务、`tools/NitroCloud.GatewaySim` 与仓库不一致——deploy.sh 未创建；当前 docker-compose.yml 无 profile/sim 服务；tools/GatewaySim 已从工作区删除（git status 显示 deleted，仍被跟踪）。根 README.md、docs/20、.gitattributes 均为未提交文件。
- 验证：重读 docs/README.md 核对每处事实（提交哈希、文件路径、ADR 目录存在性；notes/ADR/README.md 不存在，故链接指向 notes/ADR/ 目录本身）。
- 未提交 git；是否提交由用户决定（AGENTS.md 规则 6）。

